# Web Crawler & Text Summarizer

## Setup

Add modules to `FinalProject.cabal` under `build-depends:`. 

Then, to install, run:

```
cabal install
```

To build:

```
cabal build
```

And finally, to run the program:

```
cabal v2-run
```

